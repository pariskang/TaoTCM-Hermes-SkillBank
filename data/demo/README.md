# Demo data

Binary Excel workbooks are intentionally not committed to this repository.
Generate the demo workbook locally when needed:

```bash
python -m canon_tcm_hermes.demo_data
```

This creates `data/demo/shanghan_six_formula_demo.xlsx`, which is ignored by Git.
